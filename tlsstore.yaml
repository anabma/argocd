# TLSstore defining default certificate and where it's stored
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: kube-system
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  defaultCertificate:
    secretName: wildcard-cert
